#!/usr/bin/env node

var path = require('path');
var fs = require('fs');
var dir = path.join(path.dirname(fs.realpathSync(__filename)), '../');
require(dir + 'boilerplate_javascript_app.js').run();
require(dir + 'boilerplate_converted_javascript_app.js').run();